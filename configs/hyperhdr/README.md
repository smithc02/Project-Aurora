# HyperHDR configuration

Keep reviewed, user-specific HyperHDR notes or export conventions outside the
tracked repository. Milestone 16's bounded component controls are configured in
the untracked Aurora deployment file described in
[`docs/hyperhdr-controls.md`](../../docs/hyperhdr-controls.md). Do not commit
hostnames, addresses, credentials, or captures. HyperHDR remains the baseline
capture and color-extraction component.
