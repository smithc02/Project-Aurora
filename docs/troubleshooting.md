# Troubleshooting

For environment issues, confirm `uv --version`, then run `uv sync --all-groups`
and `make check`. If `uv` is unavailable, use the installation command printed
by `scripts/bootstrap.sh`. Hardware troubleshooting is not part of Milestone 1.
